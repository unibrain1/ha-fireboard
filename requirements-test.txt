# Minimal requirements for CI testing
# Core testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Code quality tools
black>=22.0.0
isort>=5.10.0
flake8>=5.0.0
flake8-docstrings>=1.7.0
flake8-bugbear>=23.0.0
flake8-comprehensions>=3.14.0
flake8-simplify>=0.19.0
mypy>=1.0.0
bandit>=1.7.0
safety>=2.3.0
vulture>=2.7.0

# Home Assistant testing (minimal)
# Note: Removed pytest-homeassistant-custom-component due to dependency conflicts
# We'll test our code directly without Home Assistant-specific testing tools

# Integration dependencies
aiohttp>=3.8.0
paho-mqtt>=1.6.0

# Skip Home Assistant installation for now - focus on basic testing
# We'll mock the Home Assistant components in tests
